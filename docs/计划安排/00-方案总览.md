# NVC 练习平台 — 方案总览

> 创建时间：2026-07-16
> 最后更新：2026-08-05
> 目标：Java 后端 + AI 应用开发简历项目

---

## 一、项目定位

基于 Spring Boot 4.0 + Spring AI 2.0 + React 18 + PostgreSQL + pgvector 的 AI 驱动非暴力沟通学习平台。作为简历项目，面向 Java 后端开发 / AI 应用开发岗位，需要同时体现**后端架构能力**和 **AI 应用开发能力**。

---

## 二、技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 4.0.1 |
| AI 框架 | Spring AI | 2.0.0-M4 |
| Agent 工具 | spring-ai-agent-utils | 0.7.0 |
| 数据库 | PostgreSQL + pgvector | pg16 |
| 缓存 | Redis (Redisson) | 4.0.0 |
| 文档解析 | Apache Tika | 2.9.2 |
| 对象存储 | RustFS (S3 兼容) | - |
| ASR/TTS | DashScope (Qwen) | 2.22.7 |
| PDF 导出 | iText | 8.0.5 |
| API 文档 | SpringDoc OpenAPI | 3.0.2 |
| 前端框架 | React + TypeScript | 18 |
| 样式 | Tailwind CSS | 4 |
| 动画 | Framer Motion | - |
| 图表 | Recharts | - |
| 前端测试 | Vitest + @testing-library/react | 3.x |
| Java | JDK | 21 |

---

## 三、整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端（React 18）                          │
│  练习中心 │ 对话页面 │ 语音练习 │ 主Agent │ Wiki │ 仪表盘 │ 场景库 │
│  Trace链路 │ 指标面板 │ 降级提示 │                                │
└────────────────────────────┬────────────────────────────────────┘
                             │ REST API + SSE + WebSocket
┌────────────────────────────┴────────────────────────────────────┐
│                     后端（Spring Boot 4.0）                       │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Controller 层 — 仅路由和委托                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Service 层 — 业务逻辑                                     │   │
│  │                                                           │   │
│  │  ┌─────────────────┐  ┌─────────────────┐               │   │
│  │  │ 练习模块         │  │ 主 Agent 模块    │               │   │
│  │  │ AgentOrchestrator│  │ AgentLoop       │               │   │
│  │  │ ModeRouter策略   │  │ 7层Hook链       │               │   │
│  │  │ 10种Agent场景    │  │ CoT引擎+工具调用 │               │   │
│  │  └─────────────────┘  └─────────────────┘               │   │
│  │                                                           │   │
│  │  ┌─────────────────┐  ┌─────────────────┐               │   │
│  │  │ Wiki 模块        │  │ RAG 检索服务     │               │   │
│  │  │ WikiService      │  │ NvcRagService    │               │   │
│  │  │ 自动沉淀+手动    │  │ 多库+个性化      │               │   │
│  │  └─────────────────┘  └─────────────────┘               │   │
│  │                                                           │   │
│  │  ┌─────────────────┐  ┌─────────────────┐               │   │
│  │  │ 评估 Service     │  │ 场景推荐服务     │               │   │
│  │  │ @Tool 注解       │  │ RecommendService │               │   │
│  │  └─────────────────┘  └─────────────────┘               │   │
│  │                                                           │   │
│  │  ┌─────────────────────────────────────────────┐         │   │
│  │  │ LLMOps 层（Phase 2 面试亮点）                │         │   │
│  │  │ MetricsCollector │ TraceManager │ FallbackHandler│     │   │
│  │  │ 指标采集         │ Trace链路    │ 降级策略       │     │   │
│  │  └─────────────────────────────────────────────┘         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  基础设施层                                                │   │
│  │  LlmProviderRegistry │ RedisService │ FileStorage        │   │
│  │  AbstractStreamProducer/Consumer (Redis Stream 异步)      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  数据层                                                    │   │
│  │  PostgreSQL + pgvector │ Redis │ 文件系统（Wiki Markdown）  │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、两层 Agent 架构（核心设计决策）

### 4.1 主 Agent（全能对话入口）

用户直接对话的智能助手，能调用所有模块工具。基于自建 AgentLoop 引擎，支持多轮工具调用、上下文压缩和 7 层 Hook 链。

```
用户: 帮我看看最近的练习情况
主Agent: [AgentLoop → dashboard_query] → "你最近一周练习了3次..."

用户: 生成一个和领导沟通的场景
主Agent: [AgentLoop → scenario_generate] → "已生成场景：..."

用户: 帮我记录一下今天的学习感悟
主Agent: [AgentLoop → wiki_write] → "已记录到你的个人 Wiki"
```

### 4.2 练习 Agent（多 Agent 编排）

专注于练习对话流程，10 种专职 Agent 通过 ModeRouter 策略路由。

```
场景驱动模式：
  CREATED → SCENARIO_GENERATOR（引入场景）
  后续 → DIFFICULT_PARTNER（扮演对方）
  评估触发 → NVC_EXPRESSION_EVALUATOR

结构化四步模式：
  OBSERVE → STEP_OBSERVE_COACH
  FEELING → STEP_FEELING_COACH
  NEED → STEP_NEED_COACH
  REQUEST → STEP_REQUEST_COACH
```

### 4.3 分工原则

| 维度 | 主 Agent | 练习 Agent |
|------|---------|-----------|
| 定位 | 全能对话入口 | 练习对话流程 |
| 工具调用 | 完整（10+ 工具） | 可选（RAG、评估 Skill） |
| 思考链 | CoT 引擎 | 无（直接调 LLM） |
| 多 Agent | 单 Agent + Hook 链 | 多 Agent 编排 |
| 用户交互 | 自然语言完成所有操作 | 角色扮演 + 教练引导 |
| 上下文管理 | LLM 摘要压缩（>20轮） | 固定窗口 |

---

## 五、实施路线图（已完成状态）

```
Phase 0：修 Bug + 稳定现有（已完成 ✅）
    ├── 核心流程修复（会话状态、Controller→Service、状态校验）
    ├── 数据与性能修复（profileSummary 注入、@Async 线程池、查询优化）
    └── Agent 路由增强（ScenarioRouter / StructuredRouter / FreeDialogRouter）
    ↓
Phase 0.5：基建补全（已完成 ✅）
    ├── KnowledgeBaseType 枚举 + 前端类型筛选
    └── 命名修复
    ↓
Phase 1：RAG + Agent 升级（已完成 ✅）
    ├── Phase 1.1 RAG 知识库集成（15 份 NVC 文档 + 种子服务 + 全模式 RAG）
    ├── Phase 1.2 Agent 工具调用框架（NvcTool + NvcToolRegistry + 15 个工具）
    └── Phase 1.3 Skill 标准化（NvcScenarioRecommendService + Service 即 Skill）
    ↓
Phase 2：主 Agent + Wiki + 面试亮点（已完成 ✅）
    ├── 主 Agent 深度改进（AgentLoop + 7层Hook + 上下文压缩 + CoT）
    ├── Wiki 模块（复用 knowledge_base 表 + 自动沉淀 + 前端页面）
    ├── NvcAssistant 全能对话入口（13 后端 + 6 前端新文件）
    ├── 质量加固（15 后端测试 + 前端基建 + Error Boundary + SSE 抽取）
    ├── IntentRouter 集成 + 面试残留清理
    └── 面试亮点补充（74 文件 +5844 行）
        ├── P0-1 量化指标采集（MetricsCollector + Golden Dataset + 评估一致性）
        ├── P0-2 Trace 可观测体系（TraceManager + 离线评估 + 前端链路页面）
        └── P0-3 LLM Fallback 降级（LlmFallbackHandler + 降级模板 + 关键词评分）
    ↓
Phase 3：工程化 + 简历包装（待推进）
    ├── 测试覆盖补充（部分已完成：15 后端测试 + 前端测试基建）
    ├── CI/CD 配置（GitHub Actions）
    └── 文档完善 + 简历最终包装
```

---

## 六、核心技术亮点（简历用）

| # | 亮点 | 技术栈 | 简历关键词 |
|---|------|--------|-----------|
| 1 | 多 Agent 编排架构 | ModeRouter 策略模式 | 10种专职Agent、动态路由、策略调度 |
| 2 | Agent 工具调用框架 | Spring AI Function Calling | 10+工具、动态注册、NvcTool接口 |
| 3 | RAG 知识检索系统 | pgvector + 混合检索 | 向量+关键词、个性化权重、多知识库 |
| 4 | 个人知识 Wiki | 文件系统 + DB + 向量 | 三层存储、AI自动沉淀、语义检索 |
| 5 | 自建 Agent Loop | Hook 链 + 上下文压缩 | 7层Hook、LLM摘要压缩、CoT引导 |
| 6 | LLM 全链路 Fallback 降级 | 规则 + 模板 + 关键词 | 3次重试、分层降级、业务不中断 |
| 7 | 全链路 Trace 可观测 | TraceManager + Redis Stream | 7节点埋点、链路回放、前端时间线 |
| 8 | 量化指标与离线评估 | Golden Dataset + LLM-as-Judge | 4项核心指标、5维度评估、低分回流 |

---

## 七、文档索引

| 文件 | 内容 |
|------|------|
| [00-方案总览.md](00-方案总览.md) | 本文档 |
| [01-现状诊断.md](01-现状诊断.md) | 当前模块状态 + 已完成清单 |
| [02-Phase0-修复计划.md](02-Phase0-修复计划.md) | Bug 修复 + 稳定化（已过时） |
| [03-Phase1-RAG与Agent升级.md](03-Phase1-RAG与Agent升级.md) | RAG 集成 + Agent 工具 + Skill（已过时） |
| [04-Phase2-主Agent与Wiki.md](04-Phase2-主Agent与Wiki.md) | 主 Agent + Wiki + 语音出题（已过时） |
| [05-Phase3-工程化与简历.md](05-Phase3-工程化与简历.md) | 测试 + 文档 + 简历 + 面试（已过时） |
| [06-技术规格.md](06-技术规格.md) | 数据模型 + API + 文件清单（已过时） |
| [07-Phase1-详细实施规划.md](07-Phase1-详细实施规划.md) | Phase 1 详细规划（已过时） |
| [08-后续实现方向规划.md](08-后续实现方向规划.md) | Phase 2/3 规划 + 技术债务（已过时） |
| [09-计划外拓展与优化方向.md](09-计划外拓展与优化方向.md) | LLMOps + 缓存 + 安全拓展（已过时） |
| [Wiki模块-实施计划.md](Wiki模块-实施计划.md) | Wiki 详细实施计划（已过时） |
| [10-主Agent深度改进计划.md](10-主Agent深度改进计划.md) | AgentLoop + Hook 链（已过时） |
| [08-Phase2-面试亮点补充规划.md](08-Phase2-面试亮点补充规划.md) | Trace + 指标 + Fallback（已过时） |
| [07-简历亮点参考.md](../简历亮点参考.md) | 简历描述 + 面试话术 |
