-- ============================================================
-- 知识库类型种子数据
-- 为现有知识库记录补充 type 字段（默认 USER_CASE）
-- ============================================================

-- 现有知识库如果没有 type，设为 USER_CASE（向后兼容）
UPDATE knowledge_bases SET type = 'USER_CASE' WHERE type IS NULL;
